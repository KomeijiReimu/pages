---
title: 分类
description: 按 content/ 目录聚合的 KomeiReimu 分类入口。
cssclasses:
  - komei-categories-index
comments: false
---

分类页按 `content/` 的一级目录生成入口，例如 `posts/` 和 `notes/`。分类名称、描述和强调色可以在 `quartz/komeireimu.config.ts` 的 `categoryLabels` 中调整。
