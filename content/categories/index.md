---
title: 分类
description: 按 notes 文件夹结构聚合的 KomeiReimu 分类入口。
cssclasses:
  - komei-categories-index
comments: false
---

分类页直接展示 `content/notes/` 下的一级文件夹结构，不混入 `posts/` 随笔，也不会把 `notes` 根目录下的单篇笔记当作分类。分类名称、描述和强调色可以在 `quartz/komeireimu.config.ts` 的 `categoryLabels` 中调整。
